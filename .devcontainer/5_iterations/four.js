const myobject = {
    js : "Javascript",
    cpp : "c++",
    rb: " ruby",
    swift : " swift by Apple"
}
for (const key in myobject) {
//    console.log(`${key} is Shortcut For ${myobject[key]}`);
   
}
const programming = ["js"," java", " c++", "rb"]
for (const key in programming) {
     console.log(`${key} for the language :${programming[key]}`);
     
}